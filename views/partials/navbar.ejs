<nav>
  <div class="container">
    <div class="navbar">
      <div class="logo">
        <% if (user) { %>
        <p>Welcome, <%= user.email %></p>
        <% } else { %>
        <a href="/">W.S.P</a>
        <% } %>
      </div>
      <div class="links">
        <ul class="navlinks">
          <% if (user) { %>
          <li><a href="load">View Load</a></li>
          <li><a href="/logout">Log Out</a></li>
          <% } else { %>
          <li><a href="login" class="login-btn">Login</a></li>
          <li><a href="register" class="login-btn">Sign Up</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </div>
</nav>
